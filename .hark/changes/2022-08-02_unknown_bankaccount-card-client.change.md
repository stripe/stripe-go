---
title: For BankAccount and Card client methods, check that exactly one of `params.Account` and `params.Customer` is set (previously they could both be set, but only one would be used, and it was different between BankAccount and Card)
is_breaking: true
section: ⚠️ Changed
released_in_version: 73.0.0
---
